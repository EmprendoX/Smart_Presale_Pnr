# Smart Pre-Sale environment example
# La base de datos de Supabase está temporalmente apagada; deja USE_SUPABASE vacío o en "false" para usar el modo JSON local.

# Supabase Configuration (habilitar solo cuando la base vuelva a estar disponible)
USE_SUPABASE=false
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Tenant Configuration
DEFAULT_TENANT_ID=tenant_default
DEFAULT_TENANT_SLUG=smart-presale
TENANT_BASE_DOMAIN=

# Stripe (opcional; el driver mock se usa por defecto)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_CONNECT_ACCOUNT_ID=
